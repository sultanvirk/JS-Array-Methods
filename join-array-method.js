const countries = ['Russia', 'USA', 'Germany', 'Australia', 'Canada']

// Join method return the values with given separator
// Example :-  array.join(separator)
// array.join(' - ')
// array.join(' , ')
// array.join('')

const res = countries.join(' - ')

console.log(res)