const numbers = [1,2,3,4,5,6]

//  The pop method removes the last item from an array
//  It returns the last item if we want then assign a variable

const lastItem = numbers.pop()

console.log(lastItem)
console.log(numbers)