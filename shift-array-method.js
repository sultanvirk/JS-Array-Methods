const numbers = [1,2,3,4,5]

// The shift method in array removes the element from biggning of array and returns it
// we can say that its the opposite of pop method

const removedElement = numbers.shift()

console.log(numbers)
console.log(removedElement)