const numbers = [1,2,3]


// unshift method is used ton add new elements in the biggning of the array and returns the new length of the array

const newLength = numbers.unshift(0,1,2,-4)

console.log(numbers)
console.log(newLength)